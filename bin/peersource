#!/usr/bin/python

import os
import sys

current_path = os.path.abspath(__file__)
current_path = os.path.dirname(current_path)
pixtream_path = os.path.join(current_path, '..')

sys.path.append(pixtream_path)

from pixtream.peer.peer import run_source
run_source()
