#!/usr/bin/python

import os
import sys

current_path = os.path.abspath(__file__)
current_path = os.path.dirname(current_path)
pixtream_path = os.path.join(current_path, '../src')

sys.path.append(pixtream_path)

from pixtream.tracker.tracker import run
run()
